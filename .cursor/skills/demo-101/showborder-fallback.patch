diff --git a/packages/grafana-ui/src/components/BigValue/BigValueLayout.test.tsx b/packages/grafana-ui/src/components/BigValue/BigValueLayout.test.tsx
index 2c333aceb76..f2a02baed30 100644
--- a/packages/grafana-ui/src/components/BigValue/BigValueLayout.test.tsx
+++ b/packages/grafana-ui/src/components/BigValue/BigValueLayout.test.tsx
@@ -101,6 +101,23 @@ describe('BigValueLayout', () => {
       expect(layout).toBeInstanceOf(WideWithChartLayout);
     });
 
+    it('should not include border styles when showBorder is unset', () => {
+      const layout = buildLayout(getProps());
+      const panelStyles = layout.getPanelStyles();
+
+      expect(panelStyles.border).toBeUndefined();
+      expect(panelStyles.borderRadius).toBeUndefined();
+    });
+
+    it('should include border styles when showBorder is true', () => {
+      const theme = createTheme();
+      const layout = buildLayout(getProps({ showBorder: true, theme }));
+      const panelStyles = layout.getPanelStyles();
+
+      expect(panelStyles.border).toBe(`1px solid ${theme.colors.border.medium}`);
+      expect(panelStyles.borderRadius).toBe(theme.shape.radius.default);
+    });
+
     it.each([
       ['wide layout', {}],
       ['non-wide layout', { disableWideLayout: true }],
diff --git a/packages/grafana-ui/src/components/BigValue/BigValueLayout.tsx b/packages/grafana-ui/src/components/BigValue/BigValueLayout.tsx
index 7fdf1f17005..32270e38655 100644
--- a/packages/grafana-ui/src/components/BigValue/BigValueLayout.tsx
+++ b/packages/grafana-ui/src/components/BigValue/BigValueLayout.tsx
@@ -230,6 +230,11 @@ export abstract class BigValueLayout {
         break;
     }
 
+    if (this.props.showBorder) {
+      panelStyles.border = `1px solid ${theme.colors.border.medium}`;
+      panelStyles.borderRadius = theme.shape.radius.default;
+    }
+
     if (this.justifyCenter) {
       panelStyles.alignItems = 'center';
       panelStyles.flexDirection = 'row';
diff --git a/packages/grafana-ui/src/components/BigValue/BigValueTypes.ts b/packages/grafana-ui/src/components/BigValue/BigValueTypes.ts
index 6c93955d618..1afd8790e36 100644
--- a/packages/grafana-ui/src/components/BigValue/BigValueTypes.ts
+++ b/packages/grafana-ui/src/components/BigValue/BigValueTypes.ts
@@ -74,4 +74,7 @@ export interface Props extends Themeable2 {
    * Disable the wide layout for the BigValue
    */
   disableWideLayout?: boolean;
+
+  /** When true, draws a themed border around the value */
+  showBorder?: boolean;
 }
diff --git a/public/app/plugins/panel/stat/StatPanel.tsx b/public/app/plugins/panel/stat/StatPanel.tsx
index 755e439d81b..cdb0a753e41 100644
--- a/public/app/plugins/panel/stat/StatPanel.tsx
+++ b/public/app/plugins/panel/stat/StatPanel.tsx
@@ -71,6 +71,7 @@ export const StatPanel = memo(
             className={targetClassName}
             disableWideLayout={!options.wideLayout}
             percentChangeColorMode={options.percentChangeColorMode}
+            showBorder={options.showBorder}
           />
         );
       },
diff --git a/public/app/plugins/panel/stat/module.tsx b/public/app/plugins/panel/stat/module.tsx
index 471e5f9a1c5..93899fe0993 100644
--- a/public/app/plugins/panel/stat/module.tsx
+++ b/public/app/plugins/panel/stat/module.tsx
@@ -112,6 +112,13 @@ export const plugin = new PanelPlugin<Options>(StatPanel)
         category: mainCategory,
         showIf: (config) => !config.reduceOptions.values,
       })
+      .addBooleanSwitch({
+        path: 'showBorder',
+        name: t('stat.name-show-border', 'Show border'),
+        description: t('stat.description-show-border', 'Draw a border around the value'),
+        defaultValue: defaultOptions.showBorder,
+        category: mainCategory,
+      })
       .addSelect({
         path: 'percentChangeColorMode',
         name: t('stat.percent-change-color-mode', 'Percent change color mode'),
diff --git a/public/app/plugins/panel/stat/panelcfg.cue b/public/app/plugins/panel/stat/panelcfg.cue
index 94ebfadc51e..b7db0375999 100644
--- a/public/app/plugins/panel/stat/panelcfg.cue
+++ b/public/app/plugins/panel/stat/panelcfg.cue
@@ -33,6 +33,7 @@ composableKinds: PanelCfg: {
 					textMode:               common.BigValueTextMode & (*"auto" | _)
 					wideLayout:             bool | *true
 					showPercentChange:      bool | *false
+					showBorder:             bool | *false
 					percentChangeColorMode: common.PercentChangeColorMode & (*"standard" | _)
 				} @cuetsy(kind="interface")
 			}
diff --git a/public/app/plugins/panel/stat/panelcfg.gen.ts b/public/app/plugins/panel/stat/panelcfg.gen.ts
index 95d8a079321..95def036a47 100644
--- a/public/app/plugins/panel/stat/panelcfg.gen.ts
+++ b/public/app/plugins/panel/stat/panelcfg.gen.ts
@@ -17,6 +17,7 @@ export interface Options extends common.SingleStatBaseOptions {
   graphMode: common.BigValueGraphMode;
   justifyMode: common.BigValueJustifyMode;
   percentChangeColorMode: common.PercentChangeColorMode;
+  showBorder: boolean;
   showPercentChange: boolean;
   textMode: common.BigValueTextMode;
   wideLayout: boolean;
@@ -27,6 +28,7 @@ export const defaultOptions: Partial<Options> = {
   graphMode: common.BigValueGraphMode.Area,
   justifyMode: common.BigValueJustifyMode.Auto,
   percentChangeColorMode: common.PercentChangeColorMode.Standard,
+  showBorder: false,
   showPercentChange: false,
   textMode: common.BigValueTextMode.Auto,
   wideLayout: true,
diff --git a/public/app/plugins/panel/stat/panelcfg.test.ts b/public/app/plugins/panel/stat/panelcfg.test.ts
new file mode 100644
index 00000000000..c04ffd22da7
--- /dev/null
+++ b/public/app/plugins/panel/stat/panelcfg.test.ts
@@ -0,0 +1,7 @@
+import { defaultOptions } from './panelcfg.gen';
+
+describe('Stat panel defaultOptions', () => {
+  it('defaults showBorder to false for backwards compatibility', () => {
+    expect(defaultOptions.showBorder).toBe(false);
+  });
+});
diff --git a/public/locales/en-US/grafana.json b/public/locales/en-US/grafana.json
index d6ef40e040f..d10fe371294 100644
--- a/public/locales/en-US/grafana.json
+++ b/public/locales/en-US/grafana.json
@@ -14977,6 +14977,7 @@
       "label-value": "Value"
     },
     "description-graph-mode": "Stat panel graph / sparkline mode",
+    "description-show-border": "Draw a border around the value",
     "description-text-mode": "Control if name and value is displayed or just name",
     "graph-mode": {
       "options": {
@@ -14988,6 +14989,7 @@
       "label": "Color mode"
     },
     "name-graph-mode": "Graph mode",
+    "name-show-border": "Show border",
     "name-show-percent-change": "Show percent change",
     "name-text-alignment": "Text alignment",
     "name-text-mode": "Text mode",
